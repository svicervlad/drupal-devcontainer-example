/**
 * @file
 * Pico HTMX behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.picoHtmx = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
